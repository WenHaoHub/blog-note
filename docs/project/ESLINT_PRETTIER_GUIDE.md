# ESLint 和 Prettier 使用指南

> 本文档介绍项目中安装的代码检查和格式化工具，帮助你理解每个工具的作用和使用方法。

---

## 📦 已安装的依赖包详解

### **核心工具包**

#### 1. ESLint (`eslint@^8.57.1`)

- **作用**：代码质量检查工具，像"代码医生"
- **功能**：
  - 找出代码中的**错误**（如未定义的变量、重复声明等）
  - 检查代码**风格不一致**（如缩进、命名规范）
  - 发现**潜在的 bug**（如在 effect 中错误调用 setState）
- **例子**：你的代码中 `fetchData` 在声明前使用，ESLint 就能检测出来
- **输出**：红色 ❌ 表示错误，黄色 ⚠️ 表示警告

---

#### 2. Prettier (`prettier@^3.7.4`)

- **作用**：代码格式化工具，像"代码美容师"
- **功能**：
  - 自动统一代码格式（引号、空格、换行等）
  - 不关心代码逻辑，只关心**外观整洁**
  - 运行一次命令，所有文件格式统一
- **例子**：把所有单引号改成双引号，把乱糟糟的代码变得整齐
- **特点**：没有配置选项的困扰，开箱即用

---

### **配置和集成包**

#### 3. eslint-config-prettier (`eslint-config-prettier@^10.1.8`)

- **作用**：配置包，协调 ESLint 和 Prettier 的冲突
- **功能**：
  - 禁用 ESLint 中与 Prettier **格式化冲突**的规则
  - 防止 ESLint 和 Prettier 为同一个格式问题互相"打架"
- **为什么需要**：
  - ESLint 想检查行长度，Prettier 也想控制行长度
  - 这个包确保只有 Prettier 负责格式，ESLint 负责代码质量
- **示意图**：
  ```
  ESLint (格式化规则) ✗ 停用
           ↓
  Prettier (格式化)   ✓ 启用
  ```

---

#### 4. eslint-plugin-prettier (`eslint-plugin-prettier@^5.5.4`)

- **作用**：让 ESLint 把 Prettier 的格式化问题当作 lint 错误报告
- **功能**：
  - 当代码不符合 Prettier 格式时，ESLint 会显示**警告 ⚠️**
  - 运行 `npm run lint:fix` 时会同时修复 ESLint 和 Prettier 问题
- **简单说**：让 ESLint 和 Prettier **无缝配合**
- **效果**：
  ```
  ESLint 发现格式问题 → 调用 Prettier 修复 → 问题解决 ✓
  ```

---

### **React 专用插件**

#### 5. eslint-plugin-react (`eslint-plugin-react@^7.37.5`)

- **作用**：React 专用的 ESLint 规则包
- **检查内容**：
  - React 最佳实践（如导入 React，JSX 中使用正确的方式）
  - 组件命名规范（组件名必须大写开头）
  - Props 验证和使用
  - 未使用的 state 和 props
- **例子**：检查你是否正确传递 Props，是否有未使用的变量等
- **典型警告**：

  ```javascript
  // ❌ 警告：组件必须大写开头
  function myComponent() {}

  // ✅ 正确
  function MyComponent() {}
  ```

---

#### 6. eslint-plugin-react-hooks (`eslint-plugin-react-hooks@^7.0.1`)

- **作用**：检查 React Hooks 的使用规范
- **检查内容**：
  - `useEffect` 依赖项是否正确
  - 不允许在**条件语句**中使用 Hooks
  - setState 是否在**正确的地方**调用
  - useCallback 和 useMemo 的依赖项
- **例子**：你的代码中，它发现 `fetchData` 在 effect 中直接调用 setState，就给出了警告
- **常见问题**：

  ```javascript
  // ❌ 错误：在条件语句中使用 Hooks
  if (condition) {
    const [state, setState] = useState(0)
  }

  // ✅ 正确：顶层使用
  const [state, setState] = useState(0)
  ```

---

## 🔄 工作流程示意图

```
你写代码
   ↓
运行 npm run lint
   ↓
ESLint + React 插件 检查 → 找到代码质量问题 → 显示错误 ❌
   ↓
运行 npm run lint:fix
   ↓
ESLint + Prettier 自动修复 → 代码变整洁且正确 ✅
```

---

## 📝 可用的 npm 命令

### 检查和修复命令

#### `npm run lint`

- **功能**：检查代码中的 ESLint 问题（不修改文件）
- **输出**：显示所有代码质量问题
- **何时使用**：
  - 在 commit 前检查代码
  - 在 CI/CD 流程中验证代码质量
  - 学习编码规范
- **示例输出**：
  ```
  E:\learn\my-app\src\App.js
    15:5  error  Cannot access variable before it is declared
  ```

#### `npm run lint:fix`

- **功能**：自动修复 ESLint 和 Prettier 问题（修改文件）
- **能修复的问题**：
  - 引号风格不一致 ✓
  - 缩进问题 ✓
  - 空格问题 ✓
  - 某些逻辑错误 ✓（取决于配置）
- **何时使用**：开发中快速修复常见错误
- **注意**：如果有无法自动修复的错误，你需要手动修改

#### `npm run format`

- **功能**：Prettier 格式化代码（修改文件）
- **格式化范围**：`src/**/*.{js,jsx,css,json}` 所有 JS、CSS、JSON 文件
- **作用**：
  - 统一所有代码的外观
  - 强制执行 `.prettierrc.json` 中定义的格式规则
- **何时使用**：在 commit 前统一代码风格
- **示例**：

  ```javascript
  // 执行前
  const x = { a: 1, b: 2, c: 3 }

  // 执行后
  const x = { a: 1, b: 2, c: 3 }
  ```

#### `npm run format:check`

- **功能**：检查代码是否符合 Prettier 格式（不修改文件）
- **何时使用**：在 CI/CD 中检查代码风格，不修改任何文件
- **输出**：列出所有不符合格式的文件

---

## ⚙️ 配置文件详解

### `.eslintrc.json` - ESLint 配置文件

```json
{
  "env": {
    "browser": true, // 浏览器环境（支持 window, document 等）
    "es2021": true, // 支持 ES2021 语法
    "node": true // Node.js 环境（支持 module, require 等）
  },
  "extends": [
    "eslint:recommended", // ESLint 推荐规则
    "plugin:react/recommended", // React 插件推荐规则
    "plugin:react-hooks/recommended", // React Hooks 推荐规则
    "prettier" // Prettier 配置（禁用冲突规则）
  ],
  "parserOptions": {
    "ecmaVersion": "latest", // 支持最新 ECMAScript
    "sourceType": "module", // 使用 ES 模块
    "ecmaFeatures": {
      "jsx": true // 支持 JSX
    }
  },
  "plugins": [
    "react", // React 插件
    "react-hooks", // React Hooks 插件
    "prettier" // Prettier 插件
  ],
  "rules": {
    "react/react-in-jsx-scope": "off", // React 19 不需要导入 React
    "react/prop-types": "off", // 不强制检查 PropTypes
    "prettier/prettier": "warn", // Prettier 问题显示为警告
    "no-unused-vars": "warn", // 未使用的变量显示为警告
    "no-console": "warn" // console 语句显示为警告
  },
  "settings": {
    "react": {
      "version": "detect" // 自动检测 React 版本
    }
  }
}
```

**规则说明**：
| 值 | 含义 |
|-----|------|
| "off" | 关闭规则 |
| "warn" | 显示警告（⚠️） |
| "error" | 显示错误（❌） |

---

### `.prettierrc.json` - Prettier 配置文件

```json
{
  "semi": true, // 语句末尾添加分号
  "singleQuote": false, // 使用双引号（不用单引号）
  "trailingComma": "es5", // 最后一个元素后添加逗号（ES5 兼容）
  "bracketSpacing": true, // 对象字面量中添加空格 { a: 1 }
  "jsxBracketSameLine": false, // JSX 闭合标签另起一行
  "arrowParens": "always", // 箭头函数参数用括号 (x) => x
  "printWidth": 80, // 单行代码最大长度 80 个字符
  "tabWidth": 2, // 缩进宽度 2 个空格
  "useTabs": false, // 使用空格而不是制表符
  "endOfLine": "lf" // 行尾使用 LF 换行符
}
```

**常见配置说明**：
| 配置项 | 作用 | 示例 |
|-------|------|------|
| `printWidth` | 一行最多字符数 | `80` = 80 字符自动换行 |
| `tabWidth` | 缩进大小 | `2` = 两个空格一级缩进 |
| `useTabs` | 用制表符还是空格 | `false` = 用空格 |
| `semi` | 语句末尾是否加分号 | `true` = `const x = 1;` |
| `singleQuote` | 用单引号还是双引号 | `false` = `"hello"` |

---

## 💡 实际工作流程

### 场景 1：修复代码错误

```bash
# 1. 检查代码
npm run lint

# 输出：
# E:\learn\my-app\src\App.js
#   15:5  error  Cannot access variable before it is declared

# 2. 自动修复
npm run lint:fix

# 输出：
# ✓ 问题已修复！

# 3. 再次检查
npm run lint

# 输出：
# （没有错误）✓
```

### 场景 2：统一代码风格

```bash
# 1. 格式化所有文件
npm run format

# 输出：
# src/App.js 32ms
# src/index.js 4ms
# src/index.css 95ms

# 2. 验证格式是否一致
npm run format:check

# 输出：
# 所有文件符合 Prettier 格式 ✓
```

---

## 🎯 ESLint 和 Prettier 的区别

| 特性         | ESLint                 | Prettier               |
| ------------ | ---------------------- | ---------------------- |
| **作用**     | 检查代码质量           | 格式化代码             |
| **关注点**   | 逻辑错误、最佳实践     | 代码外观、风格         |
| **检查项**   | 变量、函数、语法       | 缩进、引号、空格       |
| **配置多少** | 可配置（约 100+ 规则） | 少配置（约 20 个选项） |
| **自动修复** | `lint:fix` 能修复部分  | `format` 能修复大部分  |
| **示例**     | 发现未使用的变量       | 统一引号和缩进         |

**关系图**：

```
代码错误                 代码风格
   ↓                       ↓
ESLint                  Prettier
   ↓                       ↓
logic bugs              ugly formatting
```

---

## 📖 常见错误类型

### ESLint 常见错误

```javascript
// ❌ 错误 1: 变量在声明前使用
useEffect(() => {
  fetchData() // ← fetchData 还没声明
}, [])
const fetchData = () => {}

// ✅ 修复: 先声明后使用
const fetchData = () => {}
useEffect(() => {
  fetchData() // ✓
}, [])

// ❌ 错误 2: 在 effect 中错误调用 setState
useEffect(() => {
  setVal(123) // ← 没有依赖项，会反复执行
}, [])

// ✅ 修复: 在 effect 内定义函数
useEffect(() => {
  const fetchData = async () => {
    const result = await getData()
    setVal(result)
  }
  fetchData()
}, []) // ← 空依赖项，只执行一次
```

### Prettier 常见修复

```javascript
// ❌ 执行前（混乱的格式）
const obj = { a: 1, b: 2 }
const arr = [1, 2, 3]
const str = 'hello'

// ✅ 执行后（统一的格式）
const obj = { a: 1, b: 2 }
const arr = [1, 2, 3]
const str = 'hello'
```

---

## 🔗 VS Code 集成建议

为了获得最佳开发体验，建议安装以下 VS Code 扩展：

### 安装扩展

1. **ESLint** 扩展

   - 发布者：Microsoft
   - ID：`microsoft.vscode-eslint`
   - 功能：在编辑器中实时显示 ESLint 错误

2. **Prettier** 扩展
   - 发布者：Prettier
   - ID：`esbenp.prettier-vscode`
   - 功能：在编辑器中实时格式化预览和自动格式化

### 配置 VS Code (`settings.json`)

在 `.vscode/settings.json` 中添加：

```json
{
  // 设置默认格式化器为 Prettier
  "editor.defaultFormatter": "esbenp.prettier-vscode",

  // 保存文件时自动格式化
  "editor.formatOnSave": true,

  // ESLint 验证的文件类型
  "eslint.validate": ["javascript", "javascriptreact"],

  // 启用 ESLint 的格式化功能
  "eslint.format.enable": true,

  // 保存时运行 ESLint 修复
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": "explicit"
  }
}
```

### 使用效果

配置后，你就能在 VS Code 中：

- ✅ 实时看到 ESLint 错误提示（红色波浪线）
- ✅ 实时看到 Prettier 警告（黄色波浪线）
- ✅ 按 `Ctrl+S` 保存时自动格式化和修复
- ✅ 右键选择"格式化文档"快速美化代码

---

## 📚 相关文档链接

- **ESLint 官方文档**：https://eslint.org/
- **Prettier 官方文档**：https://prettier.io/
- **ESLint 配置文档**：https://eslint.org/docs/rules/
- **Prettier 配置文档**：https://prettier.io/docs/en/options.html
- **React 编码规范**：https://react.dev/learn

---

## ✅ 总结

| 工具         | 责任               | 命令               |
| ------------ | ------------------ | ------------------ |
| **ESLint**   | 检查代码质量和逻辑 | `npm run lint`     |
| **Prettier** | 格式化代码外观     | `npm run format`   |
| **同时修复** | 修复所有问题       | `npm run lint:fix` |

> 💡 **建议工作流**：
>
> 1. 开发时依赖 VS Code 扩展的实时提示
> 2. commit 前运行 `npm run lint:fix && npm run format`
> 3. CI/CD 中运行 `npm run lint` 和 `npm run format:check` 验证

---

**最后更新**：2025-12-11
